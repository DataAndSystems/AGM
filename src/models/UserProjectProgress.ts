interface UserProjectProgress {
    user?: User
    task?: any
    finished_num_today?: number
    finished_num_total?: number
    num_tofix?: number
    num_total?: number
    plan_num_today: number
}