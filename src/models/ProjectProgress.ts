interface ProjectProgress {
    user?: User
    project?: Project
    num_today?: number
    num_tofix?: number
    num_total?: number
    progress?: number
    f_num_today?: number
    f_num_total?: number
    p_num_today?: number
    p_num_total?: number
}